---
import { cn } from "@/lib/utils";
import type { HTMLAttributes } from "astro/types";

export interface Props extends HTMLAttributes<"p"> {}

const { class: className, ...rest } = Astro.props;
---

<p class={cn("my-4 leading-relaxed", className)} {...rest}>
  <slot />
</p>
