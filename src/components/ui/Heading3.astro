---
import { cn } from "@/lib/utils";
import type { HTMLAttributes } from "astro/types";

export interface Props extends HTMLAttributes<"h3"> {}

const { class: className, ...rest } = Astro.props;
---

<h3 class={cn("my-4 font-medium", className)} {...rest}>
  <slot />
</h3>
