---
import type { HTMLAttributes } from "astro/types";

export interface Props extends HTMLAttributes<"pre"> {}

const { class: className, ...rest } = Astro.props;
---

<div class="group">
  <pre class={className} {...rest}><slot /></pre>
</div>
